#Using nested for loops
print('Pattern A: ')

for i in range(1, 6):

    print("" * (6 - i) + "".join(str(j) for j in range(1, i + 1)))

# Part b: Using nested while loops
print('Pattern B: ')

i = 1

while i <= 7:

    if i % 2 != 0:
        print(' ' * (7 - i) + str(i) * i)

    i += 1
